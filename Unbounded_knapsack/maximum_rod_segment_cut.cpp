#include<bits/stdc++.h>

using namespace std;

int maximuxRodSegmentCut(int size[], int n, int s){
    // TO-DO
}

int main()
{
    int size[] = {2,1,1};
    int L = 4;
    int n = sizeof(size)/sizeof(size[0]);

    cout<<"Maximum number of segments be cut \t"<<maximuxRodSegmentCut(size, n, L);
}